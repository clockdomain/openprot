# Licensed under the Apache-2.0 license
# SPDX-License-Identifier: Apache-2.0

load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "lifecycle_sm",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "openprot_lifecycle_sm",
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//services/lifecycle/api:lifecycle_api",
    ],
)

# Host-side integration test: drives the run-loop with an in-memory queue and a
# scripted Actions double. No pw_kernel, no hardware.
rust_test(
    name = "lifecycle_sm_integration_test",
    srcs = ["tests/lifecycle.rs"],
    crate_root = "tests/lifecycle.rs",
    edition = "2024",
    deps = [
        ":lifecycle_sm",
        "//services/lifecycle/api:lifecycle_api",
    ],
)
